// KPIs content intentionally erased 2026-09-10 (Jas) — the old view showed
// revenue in plain text, and this tab is reachable by anyone with owner
// access, including a click-through demo. Redesign pending; no fetch, no
// dollar figures, until then. Do NOT restore the old revenue table without
// an explicit new design from Jas.
export function Kpis() {
  return (
    <div className="card dim">
      KPIs is being redesigned. Nothing to show yet.
    </div>
  );
}
